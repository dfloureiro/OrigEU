// Copy this file to lib/config.js and fill in your own deployed backend
// URL (see backend/README.md for how to deploy one). lib/config.js is
// gitignored, so your real URL is never committed — only this placeholder
// template is.
export const BACKEND_URL = 'https://origeu-api.YOUR-SUBDOMAIN.workers.dev';
